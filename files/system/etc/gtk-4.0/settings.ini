# GTK 4 system input-method fallback for Qubix OS (DD-050).
# Native Wayland GTK prefers its built-in text-input path; X11/XWayland GTK reads this
# fallback and uses the packaged Fcitx module. A user's settings.ini has higher priority.

[Settings]
gtk-im-module=fcitx
